namespace dev.sudohub.normalprocessor
{
    public enum FormatPainterState
    {
        Inactive,
        Requested,
        Active
    }

    public enum BackgroudType
    {
        Black,
        Gray,
        White
    }

    public enum TextureType
    {
        Input,
        Gauss,
        Normal,
        InputFull,
        GaussFull,
        NormalFull
    }
}